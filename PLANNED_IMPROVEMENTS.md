# Planned Improvements (Step-by-Step)

## Implemented (as of 2026-01-29)
- Lead qualification fields (intent, priority, status) stored with widget leads.
- Lead notifications (email + webhook) with qualified-only option.
- Action templates by industry (admin + customer) with inactive defaults.
- Action execution logging with API retries.
- Live data validation rules (no hallucinated prices).
- Intent keyword templates expanded with FAQ/general info keywords.
- Chat persistence fixed + configurable browser TTL for chat history.
- Admin chat history actions: reliable view + export.
- Magento extension + Composer package + Marketplace listing assets.

## Phase 1 — Lead Capture & Routing (High ROI)
1. Lead qualification rules per intent (priority/status).
2. Lead routing notifications (email + webhook/CRM).
3. Lead enrichment (location + session metadata).

## Phase 2 — Intent-Based Actions
4. Action templates by industry (prebuilt workflows).
5. Action success/failure logging with retries.
6. Live data validation rules (no hallucinated prices).

## Phase 3 — Business-Aware Responses
7. Location/time-aware answers (store hours, holidays).
8. Offer/seasonal context (date-bound promotions).
9. Product/service availability from DB sync.

## Phase 4 — Analytics & Insights
10. Intent distribution dashboard.
11. Unanswered question tracker.
12. Lead conversion funnel metrics.

## Phase 5 — Human Handoff
13. Human escalation triggers (low confidence, complaints).
14. Agent summary handoff (auto conversation summary).
15. Multi-channel handoff (WhatsApp/email/phone).

## Phase 6 — Platform Integrations
16. Magento extension (download + admin config).
17. WooCommerce/WordPress marketplace listing.
18. Shopify app store submission (compliance checklist).

## Phase 7 — Quality & Safety
19. Verified-only answer mode.
20. Response guardrails by category (legal/medical/finance).
21. Per-org approval workflow for sensitive data.

## Phase 8 — UX Polish
22. Multi-language + tone presets.
23. Smart follow-up prompts.
24. Proactive suggestions based on intent.
